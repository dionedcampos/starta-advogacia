<section class="why-us section-padding" id="about">
  <div class="container why-us-grid">
    <div class="why-us-content" data-animate>
      <span class="section-subtitle">Diferenciais Estratégicos</span>
      <h2 class="section-title">Valores que <span class="text-gold">Fundamentam</span> nossa Atuação Jurídica</h2>
      <p class="section-desc">GH Advocacia une tradição e inovação para oferecer uma defesa sólida, pautada pela transparência e pela busca incessante da justiça.</p>
      
      <ul class="values-list">
        <li>
          <div class="value-icon">
            <i class="fa-solid fa-award" aria-hidden="true"></i>
          </div>
          <div>
            <h4>Excelência Técnica</h4>
            <p>Corpo jurídico altamente qualificado com foco em soluções resolutivas.</p>
          </div>
        </li>
        <li>
          <div class="value-icon">
            <i class="fa-solid fa-scale-balanced" aria-hidden="true"></i>
          </div>
          <div>
            <h4>Ética e Transparência</h4>
            <p>Compromisso inegociável com a integridade e clareza informativa.</p>
          </div>
        </li>
        <li>
          <div class="value-icon">
            <i class="fa-solid fa-comment-dots" aria-hidden="true"></i>
          </div>
          <div>
            <h4>Atendimento Personalizado</h4>
            <p>Cada cliente recebe uma estratégia única e atenção dedicada.</p>
          </div>
        </li>
      </ul>
    </div>
    
    <div class="why-us-image" data-animate>
      <div class="image-stack">
        <div class="image-box main-img"></div>
        <div class="image-box accent-box"></div>
      </div>
    </div>
  </div>
</section>

<style>
  .why-us {
    background-color: var(--bg); /* Fundo Claro */
    color: var(--text);
  }

  .why-us-grid {
    display: grid;
    grid-template-columns: 1.15fr 0.85fr;
    gap: 6rem;
    align-items: center;
  }

  .section-subtitle {
    display: block;
    color: var(--primary);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 3px;
    font-size: 0.8rem;
    margin-bottom: 1.5rem;
  }

  .section-title {
    font-size: clamp(2.2rem, 5vw, 3.2rem);
    margin-bottom: 2rem;
    color: var(--text); /* Texto Escuro */
    font-family: var(--font-serif);
    line-height: 1.1;
  }

  .section-desc {
    font-size: 1.15rem;
    color: var(--text-muted); /* Texto Muted Escuro */
    margin-bottom: 3.5rem;
    line-height: 1.7;
  }

  .values-list {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }

  .values-list li {
    display: flex;
    gap: 1.8rem;
  }

  .value-icon {
    width: 44px;
    height: 44px;
    background-color: var(--bg-offset); /* Fundo sutil */
    color: var(--primary);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    font-size: 1.2rem;
    border: 1px solid var(--primary);
  }

  .values-list h4 {
    font-size: 1.35rem;
    margin-bottom: 0.6rem;
    color: var(--text);
    font-family: var(--font-serif);
  }

  .values-list p {
    color: var(--text-muted);
    font-size: 1rem;
    line-height: 1.6;
  }

  .image-stack {
    position: relative;
    height: 550px;
  }

  .main-img {
    width: 90%;
    height: 90%;
    background-image: url('https://images.unsplash.com/photo-1505664194779-8beaceb93744?auto=format&fit=crop&q=80&w=800');
    background-size: cover;
    background-position: center;
    position: relative;
    z-index: 2;
    box-shadow: var(--shadow-lg);
    border-left: 8px solid var(--primary);
  }

  .accent-box {
    position: absolute;
    bottom: -20px;
    right: -20px;
    width: 80%;
    height: 80%;
    border: 2px solid var(--primary);
    z-index: 1;
  }

  @media (max-width: 992px) {
    .why-us-grid {
      grid-template-columns: 1fr;
      gap: 5rem;
    }
    .image-stack {
      height: 450px;
    }
    .accent-box {
      bottom: 10px;
      right: 10px;
    }
  }
</style>
